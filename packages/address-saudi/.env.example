# Optional. Enables SPL short-address resolution and official verification.
NATIONAL_ADDRESS_API_KEY=

# Optional. Used only when the SPL adapter is enabled.
NATIONAL_ADDRESS_BASE_URL=https://api.address.gov.sa

# Optional. Default false: warn/flag invalid addresses but allow checkout.
SAUDI_ADDRESS_STRICT=false
