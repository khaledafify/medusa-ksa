# Moyasar API keys — copy from the Moyasar Dashboard → Settings → API Keys
# (https://dashboard.moyasar.com). Sandbox vs live is auto-detected from the
# key prefix (sk_test_… vs sk_live_…) — there is no mode flag.
MOYASAR_SECRET_KEY=sk_test_your-key-here
MOYASAR_PUBLISHABLE_KEY=pk_test_your-key-here

# Webhook shared secret — the same value you enter when registering the
# webhook endpoint in the Moyasar Dashboard → Settings → Webhooks.
# Optional, but strongly recommended in production.
MOYASAR_WEBHOOK_SECRET=
